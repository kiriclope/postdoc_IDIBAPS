def bin_data(data, axis, binstep, binsize):
    bin_array = [np.mean(np.take(data,np.arange(int(i*binstep),int(i*binstep+binsize)), axis=2), axis=2) for i in np.arange(data.shape[2]//binstep)-1]
    bin_array = np.array(bin_array)
    bin_array = np.rollaxis(out,0,3)
    return out
