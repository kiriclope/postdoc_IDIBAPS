import sys, os, importlib
import numpy as np
import matplotlib.pyplot as plt

import constants
from constants import *

from importlib import reload
importlib.reload(sys.modules['constants'])

print(path)

filter_rates = np.loadtxt(path + 'filter_rates.dat') ;
print('filter_rates')
print(filter_rates.shape)

time = filter_rates[:,0]
print(time)

rates = np.mean(np.delete(filter_rates,[0],axis=1),axis=0)
print(rates.shape)

xi = np.loadtxt(path + 'low_rank_xi.dat') ;
print('xi')
print(xi.shape)

kappa = np.dot(xi, rates)
print(kappa)
